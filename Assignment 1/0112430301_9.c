/*Program that will declare a variable from each data type: double, boolean. Then it will *
initialize them with values and print them.*/

#include <stdio.h>
#include <stdbool.h>

int main (void) {
    double i;
    bool j;
    i =  1.618039;
    j = 0;
    printf("The double value: %lf\nThe boolean value: %d", i, j);
}